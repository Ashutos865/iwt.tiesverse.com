{
  "name": "iwt-summit",
  "version": "1.0.0",
  "private": true,
  "description": "IWT Summit — registration, approval and QR pass system",
  "scripts": {
    "dev": "concurrently -n server,client -c yellow,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "build": "node scripts/check-fonts.mjs && npm run build --prefix client",
    "start": "npm start --prefix server"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
